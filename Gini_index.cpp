#include <iostream>



/*
PsuedoCode:
    ?This is the PsuedoCode FOr the Gini index

    Function Gini_index:

        Gini_index= 0
        For each class i:
            Gini_Index += (class_counts(i)/len(dataset))**2


        return 1.0 - Gini_index

    Function Gini_score:
        Gini_score= 0
        For each class i:
            Gini_score += (class_counts(i)/len(dataset))**2

        
    return Gini_score



*/
    